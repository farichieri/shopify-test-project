{% schema %}
{
  "name": "Shop By Bed Type",
  "settings": [
    {
      "type": "header",
      "content": "Shop By Bed Type"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Shop By Bed Type"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Choose your preferred bed type"
    },
    {
      "type": "collection",
      "id": "collections",
      "label": "Select collections",
      "limit": 4
    }
  ]
}
{% endschema %}

{% assign collections = section.settings.collections %}

{% for collection in collections %}
  <div class="collection">
    <h2>{{ collection.title }}</h2>
    <a href="{{ collection.url }}">
      <img src="{{ collection.image | img_url: '480x480' }}" alt="{{ collection.title | escape }}">
    </a>
    asd
  </div>
{% endfor %}